spring.application.name = nirogi
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.profiles.active=local

# JWT Configuration - 256-bit secret (32 chars minimum for HS256)
# Override with environment variable: JWT_SECRET
app.jwt.secret=${JWT_SECRET:dev-secret-key-change-in-production-min-32-chars-xxxxxxxx}
